# version: '3.1'

# services:
#   app:
#     build: .
#     image: acp-front-image
#     container_name: acp-front
#     restart: always
#     ports:
#       - "3006:3000"
#     networks:
#       - proxynet

# networks:
#   proxynet:
#     external: true

services:
  acp-front:
    image: $IMAGE_NAME:$IMAGE_TAG
    container_name: $APP_NAME
    restart: always
    networks:
      proxynet:

networks:
  proxynet:
    external: true
    name: proxynet