# version: '3.1'

# services:
#   app:
#     build: .
#     image: azko-front-image
#     container_name: azko-front
#     restart: always
#     networks:
#       - proxynet

# networks:
#   proxynet:
#     external: true

services:
  azko-front:
    image: ghcr.io/threestepsllc/azko-front:${IMAGE_TAG}
    container_name: azko-front
    restart: always
    networks:
      proxynet:

networks:
  proxynet:
    external: true
    name: proxynet
