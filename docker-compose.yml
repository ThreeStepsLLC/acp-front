version: '3.1'

services:
  app:
    build: .
    image: afmpro-front-image
    container_name: afmpro-front
    restart: always
    ports:
      - "3006:3000"
    networks:
      - proxynet

networks:
  proxynet:
    external: true
